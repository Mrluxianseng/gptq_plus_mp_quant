# Adam 优化器分析与 PTQ 场景改进方向

---

## 一、Adam 优化器算法梳理

**核心思想**：给每个参数维度自适应学习率，梯度大的维度步子小，梯度小的维度步子大。

**完整更新流程**（每步 t）：

```
输入：当前梯度 g_t，学习率 lr，超参 β1=0.9, β2=0.999, ε=1e-8

Step 1  一阶矩（动量）：
        m_t = β1 · m_{t-1} + (1 - β1) · g_t

Step 2  二阶矩（梯度方差，用来估计曲率）：
        v_t = β2 · v_{t-1} + (1 - β2) · g_t²

Step 3  Bias Correction（修正早期步数的偏差）：
        m̂_t = m_t / (1 - β1^t)
        v̂_t = v_t / (1 - β2^t)

Step 4  参数更新：
        w ← w - lr · m̂_t / (√v̂_t + ε)
```

**三个关键超参的含义**：

- **β1 = 0.9**：动量平滑系数，"记住"过去 ~10 步的梯度方向
- **β2 = 0.999**：曲率平滑系数，"记住"过去 ~1000 步的梯度方差
- **ε = 1e-8**：防止除零的阻尼项，同时控制最大步长

---

## 二、Adam 在 GPTQ+ 项目中的不适配问题

### 问题 1：β 参数为大步数场景设计，PTQ 场景步数极少

Adam 的 β2=0.999 是为训练 **10,000+ 步**设计的。而在 GPTQ+ 的 block_gd 模式里，每个 block 量化后只做 **10~100 步**梯度更新。

数学上，第 t 步的 bias correction 分母：

```
1 - β2^t = 1 - 0.999^50 ≈ 0.049
```

这意味着在整个量化过程中，v̂_t 始终只有真实梯度方差的 **5%**，严重低估曲率，步长被大幅高估。Adam 的自适应能力在这里几乎失效。

---

### 问题 2：Hessian 已有，但 Adam 视而不见

Adam 用 `v_t`（梯度平方的指数移动平均）**统计估计**曲率。  
但 GPTQ+ 在量化前已经精确计算了 **Hessian 矩阵 H**，这是比 `v_t` 精确得多的曲率信息。

Adam 完全没有利用这个已有信息，相当于"有精确地图不用，非要靠估算导航"。

---

### 问题 3：optimizer state 被分块重置，动量积累被打断

每个 block 量化完成后，对应列的 Adam 状态（`exp_avg`、`exp_avg_sq`）被清零，但 `step` 计数器却继续增长，导致 bias correction 使用的步数值和实际有效更新次数**不一致**，动量机制的理论假设被破坏。

---

### 问题 4：不感知量化格点，步长缺乏几何意义

Adam 的步长完全由梯度统计决定，不知道每个权重当前距最近量化格点有多远。

- 权重**远离**格点边界 → 更新不会改变量化整数值 → 可以大步走
- 权重**靠近**格点边界 → 一步可能导致量化结果翻转 → 应该谨慎

Adam 对这两种情况一视同仁，要么过于保守，要么引入不必要的量化误差。

---

### 问题 5：额外显存开销

Adam 需要存储 `exp_avg`（m_t）和 `exp_avg_sq`（v_t）两个与权重同形的张量，每个线性层需要**额外 2× 权重大小的显存**，加重了量化场景本已紧张的显存压力。

---

## 三、后续可尝试的优化方向

### 方向 1：少步数 β 自适应校准

**对应问题 1**

根据预设的总更新步数 T，反推使 bias correction 合理的最优 β2：

```
β2_optimal = 1 - 1/T
```

以 T=50 为例：β2=0.98，此时 1 - 0.98^50 ≈ 0.636，方差估计接近真实值。  
β1 同理可按"有效记忆窗口"而非固定值设定。

这一方向有明确的数学推导支撑，可作为理论贡献写入论文。

---

### 方向 2：H-Adam —— 用 Hessian 对角替换二阶矩

**对应问题 2 + 问题 5**

将 Adam 中统计估计的曲率 `v_t` 替换为 GPTQ 已计算好的精确 Hessian 对角：

```
# 原 Adam
update = lr · m̂_t / (√v̂_t + ε)        ← v̂_t 是统计估计

# H-Adam（提议）
update = lr · m̂_t / (√diag(H) + ε)    ← diag(H) 是精确曲率，已有
```

**一举两得**：
- 曲率更准确（精确 Hessian vs 统计估计）
- 去掉 `exp_avg_sq`，显存从 2× 降至 1× 权重大小

理论上这是 **Diagonal Natural Gradient** 的一个实例，有完善的收敛理论可引用。

---

### 方向 3：重新设计 step 计数与 bias correction 的对应关系

**对应问题 3**

每个 block 的 optimizer state 被重置后，对应列应视为"从第 1 步重新开始"，而非沿用全局 `step` 计数器。

改进方案：为每列（或每列段）维护独立的有效步数，确保 bias correction 始终反映该列真实的更新历史，而非全局步数。

---

### 方向 4：量化格点感知自适应阻尼

**对应问题 4**

将每个权重到最近量化格点的距离引入阻尼项：

```
distance = |w - q_nearest| / scale    ← 已在代码中计算
ε_adaptive = ε_base / (distance + δ)  ← 靠近格点时 ε 增大，步长自动缩小
```

效果：权重越靠近格点边界，更新越保守；远离边界时允许更大幅度调整。  
这赋予了优化器真正的"量化感知"能力，是 PTQ 场景独有的优化机会。

---

## 四、综合对比总结

| 问题 | Adam 的做法 | 该场景实际需求 | 对应优化方向 |
|------|------------|--------------|------------|
| 步数适配 | β2=0.999，假设 1000+ 步 | 每 block 只有 10~100 步 | 方向 1：β 自适应校准 |
| 曲率来源 | 梯度方差统计估计 | 精确 Hessian 已计算好 | 方向 2：H-Adam |
| 显存开销 | 2× 权重显存 | 量化场景显存本已紧张 | 方向 2：H-Adam（去掉 exp_avg_sq）|
| 状态连续性 | 假设全程连续积累 | 逐 block 重置，不连续 | 方向 3：独立步数计数 |
| 量化感知 | 无，纯梯度驱动 | 需感知格点距离 | 方向 4：格点感知阻尼 |

**目标**：将以上四个方向整合为一个面向 PTQ 精化场景的专用优化器，系统性地替代 Adam 在 GPTQ+ 中的角色。
